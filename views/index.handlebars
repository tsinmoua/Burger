<h1>BURGERS, BURGERS, BURGERS, AND MORE BURGERS!!!</h1>
<br>

<div class="container col-sm">
    <div class="row">
        <div class="col-sm column1">
            <h2>Are you eating that burger?</h2>
            <p>(Click the burger to devour it!)</p>
            <br>
            <ul style="list-style-type:none;">
                {{#each burgers}}
                {{#unless devoured}}
                <li class="notDevoured">
                        {{ burger_name }}
                        <input type="image" src="http://clipart-library.com/img1/1241371.png" alt="Hamburger"
                            class="devour devourButton" data-id="{{id}}" data-newStatus="{{devoured}}"
                            {{#if devoured}}""{{else}}DEVOUR!{{/if}} value="DEVOUR IT!"/>
                        
                    </button>

                </li>
                {{/unless}}
                {{/each}}
            </ul>
        </div>

        <div class="col-sm">
            <img src="https://lh3.googleusercontent.com/proxy/5_1GQ70ERu7_chHn1ZuaH-TKXxU_8WGkVKme8oI3MIcbikOpK8By19xAgVrve8_en1xLnBrId-wZ2Dv0-DTPM-8aqw"
                alt="Burger" class="burgerPic center">
        </div>


        <div class="col-sm column2">
            <h2>Yum, these were delicious!</h2>
            <br>
            <br>
            <ul style="list-style-type:none;">
                {{#each burgers}}
                {{#if devoured}}
                <li>
                    <img src="https://pngimg.com/uploads/pacman/pacman_PNG79.png" alt="Open Mouth" class="devourButton">
                        {{ burger_name }}
                </li>
                {{/if}}
                {{/each}}
            </ul>
        </div>

    </div>
</div>

<div class="container addABurger">
    <h2>Add a Burger</h2>
    <form class="create-form">
        <div class="form-group">
            <input type="text" id="burger" name="name" placeholder="Burger Name">
        </div>
        <button type="submit" id="submitButton">Add Burger</button>
</div>

</form>