<h1>BURGERS, BURGERS, BURGERS, AND MORE BURGERS!!!</h1>

<div class="container col-sm">
    <div class="row">
        <div class="col-sm">
            <h2>Why haven't you ate these burgers yet?</h2>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                <li> {{ burger_name }}
                    <input type="image"
                        src="http://clipart-library.com/img1/1241371.png" 
                        alt="Hamburger" 
                        class="devour devourButton"
                        data-id="{{id}}" 
                        data-newStatus="{{devoured}}"
                        {{#if devoured}}""{{else}}DEVOUR!{{/if}}/>DEVOUR IT!
                </li>
                {{/unless}}
                {{/each}}
            </ul>
        </div>

        <div class="col-sm">
            <img src="https://lh3.googleusercontent.com/proxy/5_1GQ70ERu7_chHn1ZuaH-TKXxU_8WGkVKme8oI3MIcbikOpK8By19xAgVrve8_en1xLnBrId-wZ2Dv0-DTPM-8aqw"
                alt="Burger" class="burgerPic center">
        </div>
        

        <div class="col-sm">
            <h2>Yum, these were delicious!</h2>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                <li> {{ burger_name }} </li>
                {{/if}}
                {{/each}}
            </ul>
        </div>

    </div>
</div>

<div class="container addABurger">
    <h2>Add a Burger</h2>
    <form class="create-form">

        <div class="form-group">
            <label for="ca">Burger Name:</label>
            <input type="text" id="ca" name="name">
        </div>

        <div class="form-group">
            <label for="devoured">Devoured?</label><br>
            <input type="radio" name="devoured" value="1" checked> Devoured<br>
            <input type="radio" name="devoured" value="0"> Not Yet
        </div>

        <button type="submit">Add Burger</button>
</div>

</form>